import React from 'react';

const Favorites = () => {
    return (
        <section className={"favorites"}>
            <div className="container">
                Избранный
            </div>
        </section>
    );
};

export default Favorites;